language: cpp

compiler:
  - clang
  - gcc

before_script:
  - sudo apt-get update -qq
  - sudo apt-get install -y libtesseract-dev libleptonica-dev
  - mkdir build
  - cd build
  - cmake ..

script: make -j VERBOSE=1 && ./travis-ci-cpp
